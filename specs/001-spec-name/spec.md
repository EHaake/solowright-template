# Spec: [Feature Name]

**Status**: Draft — pending review
**Depends on**: [nothing, if this is the first spec — otherwise name the
spec(s) this needs to already exist]

## Summary

<!-- Two or three sentences. What this feature is and why it exists,
for someone with zero other context. -->

## Goals

<!-- Numbered list. Each goal should be checkable — "let a user do X" is
checkable; "improve the experience" is not. -->

## Non-goals (explicitly deferred)

<!-- Just as important as goals. For each: what it is, and *why* it's
deferred — not just "not now" but the actual reason (needs a design
decision, needs external dependency, genuinely bigger scope than this
spec). A non-goal without a stated reason invites someone to
accidentally build it "since nothing said not to." -->

## Entities

<!-- Conceptual description of the data involved — fields and what they
mean, not types or schema. The actual data model belongs in plan.md;
this stays at the "what does the user think of as a thing" level. -->

## Key user flows

<!-- One subsection per flow, in plain prose: what the user does, what
they see, what happens. This is the section worth the most iteration —
ambiguity here becomes an implementation guess later, so push on it now. -->

## Design requirements

<!-- If there's a UI: the bar both design and implementation need to
clear, stated as requirements rather than left implicit. Empty states,
loading states, and the most frequent action deserve explicit mention —
they're easy to treat as an afterthought and they're used constantly. -->

## Acceptance criteria

<!-- Checklist. Each item should be something a person could literally
check off after using the built feature — not a restatement of the
goals in checkbox form. -->

## Resolved decisions

<!-- Running log of things that came up as ambiguous during planning and
got settled. Keep this even after the spec is "done" — it's the record
of *why* a specific choice was made, for whoever reads this in six
months. -->
